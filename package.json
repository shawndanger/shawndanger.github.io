{
  "scripts": {
    "docs:dev": "vitepress dev docs --host --cors --port 5173",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs --port 5170",
    "d": "vitepress dev docs --host --cors --port 5173",
    "b": "vitepress build docs",
    "v": "vitepress preview docs --port 5170"
  },
  "devDependencies": {
    "vitepress": "^1.1.3",
    "yarn": "^1.22.22"
  }
}
