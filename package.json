{
  "scripts": {
    "docs:dev": "vitepress dev docs --host --cors --port 5173",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs --port 80",
    "d": "vitepress dev docs --host --cors --port 5173",
    "b": "vitepress build docs",
    "v": "vitepress preview docs --port 80"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "vitepress": "latest",
    "yarn": "1.22.22"
  }
}
